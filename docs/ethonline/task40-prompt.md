# Public build brief — Task 40

Compose a reusable Uniswap v4 Initialize stream with The Graph's live Substreams provider and Pinax Token API on Base. Return pool identity from `PoolManager.Initialize`, add token meaning from the shared Token API schema, and apply transparent rules with explicit PASS, REJECT, and UNKNOWN states. Keep swaps, liquidity, wallets, transactions, provider publication, and private operational data out of scope.

AI-assisted development is disclosed in the README and specification. The owner must record the demo with a real voice; no generated narration or artificial speed-up is part of the artifact.
